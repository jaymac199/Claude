# Copy this file to ".env" and add your Twelve Data API key to enable live market data.
# Get a free key at https://twelvedata.com/ (free tier: 800 requests/day, 8 requests/min).
# Without a key, the app runs on built-in demo (mock) data.
VITE_MARKET_DATA_API_KEY=
